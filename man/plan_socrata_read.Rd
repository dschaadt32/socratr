% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/socratr.R
\name{plan_socrata_read}
\alias{plan_socrata_read}
\title{Build a smart read plan for a Socrata dataset}
\usage{
plan_socrata_read(
  url,
  domain = NULL,
  soql = "SELECT *",
  app_token = NULL,
  socrata_user = NULL,
  password = NULL,
  page_size = NULL,
  max_rows = Inf,
  format = c("auto", "json", "csv"),
  parallel = "auto",
  max_active = NULL,
  coerce = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{url}{Character. A full Socrata dataset URL \strong{or} a bare 4x4
ID (e.g. \code{"abcd-1234"}). If a bare ID, \code{domain} is also required.}

\item{domain}{Character. Required only when \code{url} is a bare 4x4 ID.
Leading \verb{https://} and trailing paths are stripped automatically.}

\item{soql}{Character. A plain SoQL query string (default
\code{"SELECT *"}). Do \strong{not} include \code{LIMIT} or \code{OFFSET}; pagination is
handled internally. Example:
\code{"SELECT name, value WHERE value > 100 ORDER BY value DESC"}.}

\item{app_token}{Character (optional). Socrata Application Token, sent as
the \code{X-App-Token} header. Strongly recommended - raises the anonymous
rate limit significantly. Falls back to \code{SOCRATA_APP_TOKEN} /
\code{SOCRATA_TOKEN}.}

\item{socrata_user}{Character (optional). Socrata account email or API Key
ID. Required for private datasets. Falls back to \code{SOCRATA_USER}.}

\item{password}{Character (optional). Socrata password or API Secret Key.
Falls back to \code{SOCRATA_PASSWORD} / \code{SOCRATA_KEY}.}

\item{page_size}{Integer. Rows fetched per request. If \code{NULL}, chosen by
the read plan (5 000 JSON / 50 000 CSV; max 50 000).}

\item{max_rows}{Integer or \code{Inf} (default). Hard cap on total rows
returned. Useful for sampling or testing without pulling a full dataset.}

\item{format}{\code{"auto"} (default), \code{"json"} (SODA 3), or \code{"csv"} (SODA 2).}

\item{parallel}{Logical or \code{"auto"}. Whether to use concurrent page
requests. Default \code{"auto"}.}

\item{max_active}{Integer or \code{NULL}. Concurrent connections when parallel.
If \code{NULL}, chosen from credentials (10 with auth, else 3).}

\item{coerce}{Logical. If \code{TRUE}, fetch schema metadata and coerce
columns to native R types (dates -> \code{POSIXct}, numbers -> \code{numeric},
checkboxes -> \code{logical}), like RSocrata's automatic date conversion.
Default \code{FALSE} (all columns remain character).}

\item{verbose}{Logical. If \code{TRUE}, prints progress and the chosen plan
(default \code{FALSE}).}
}
\value{
A named list describing the chosen strategy. Useful fields:
\code{format}, \code{parallel}, \code{page_size}, \code{max_active}, \code{row_count_est},
\code{reasons}, \code{credentials}, and optionally \code{meta}.
}
\description{
Inspects credentials, SoQL shape, and (when possible) approximate row count
to choose JSON vs CSV and sequential vs parallel fetching.
}
\details{
Decision rules (when \code{format} / \code{parallel} are \code{"auto"}):
\itemize{
\item \strong{CSV} when the result looks large (>= 5 000 rows) and SoQL is
CSV-friendly (no \verb{GROUP BY} / aggregates). Otherwise \strong{JSON}.
\item \strong{Parallel} when the result looks large (>= 10 000 rows).
\item \strong{max_active} is 10 with an app token or basic auth, else 3.
\item \strong{page_size} defaults to 50 000 for CSV and 5 000 for JSON.
}

Credentials fall back to \code{SOCRATA_APP_TOKEN} / \code{SOCRATA_TOKEN},
\code{SOCRATA_USER}, and \code{SOCRATA_PASSWORD} / \code{SOCRATA_KEY} when arguments
are omitted.
}
\examples{
\dontrun{
plan_socrata_read(
  "https://data.somervillema.gov/resource/4pyi-uqq6",
  app_token = Sys.getenv("SOCRATA_APP_TOKEN")
)
}
}
